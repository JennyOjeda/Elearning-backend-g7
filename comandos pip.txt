pip list > lista todas las liberias que tenemos instaladas
python -m venv NOMBRE > creara un entorno virtual en la ubicacion dende estemos 

Nombre/Scripts/activate > levanta el entorno virtual en la terminal si esta es CMD, powershell o terminal de windows.

sourse NOMBRE/Scripts/activate > levanta el entorno virtual en la terminal si esta es GIT BASH, terminales de S.O. linux o macOS o Linux

pip freeze > lista solamente las librerias instaladas  manualmente 

pip freeze > requirements.txt >> guarda el ouput de este comando en el archivo requeriments.txt

pip install -r requirements.txt > instala todas las librerias usando como fuente el archivo donde ñistamos todas las librerias

deactivate > desactiva el entonrno virtual 
